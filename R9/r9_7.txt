練習問題2の論理的意味と動作についてレポート

(i)r9(1)のデータベースに対して:- female(pam).を実行した結果およびその理由．

実行結果：false
理由：データベースにfemale(pam)がなかったため

(ii)実行・理由

実行結果：true
理由：データベースにfemale(pam)がなかったがfemale(X) :- not(male(X)).を追加することで、maleのデータベースにpamがなければ真であるという記述をしたため


